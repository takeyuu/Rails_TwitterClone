<%= form_with(model: @post, local: true, url: confirm_new_or_edit, method: confirm_form_method) do |f| %>
  <%= f.hidden_field :content %>
  <p>内容：<%= @post.content %></p>
  <%= f.submit 'つぶやく' %>
  <%= f.submit '戻る', name: 'back' %>
<% end %>
